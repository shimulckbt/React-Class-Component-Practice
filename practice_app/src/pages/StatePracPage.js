import React, { Component } from 'react'
import MoreStatePrac from '../Components/MoreStatePrac'

export default class StatePracPage extends Component {
    render() {
        return (
            <div>
                <MoreStatePrac />
            </div>
        )
    }
}
